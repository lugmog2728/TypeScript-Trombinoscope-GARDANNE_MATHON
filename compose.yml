services:
  run:
    image: node:20
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    working_dir: /app
    command: npm run start
    environment:
      - NODE_ENV=development
